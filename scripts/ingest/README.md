# RiftIntel ingest pipeline

Pulls League of Legends patch notes from the free League wiki MediaWiki API,
structures them, and optionally writes local Ollama gameplay summaries ($0).

See root `README.md` for commands (`npm run ingest`, etc.).
